---
# Required by the amazon.aws.aws_ssm connection plugin for temporary
# Ansible module/file transfer.
#
# Replace this placeholder with the dedicated transfer bucket for the
# target environment.
ansible_aws_ssm_bucket_name: "REPLACE_WITH_ANSIBLE_SSM_TRANSFER_BUCKET"

ansible_aws_ssm_region: "eu-west-1"
