---
# Example account inventory metadata.
# Replace placeholder values in a real deployment.
#
# Do not store credentials or secrets in this repository.

aws_accounts:
  - name: production
    account_id: "111111111111"
    role_name: DiskMonitoringAutomationRole
    regions:
      - eu-west-1

  - name: development
    account_id: "222222222222"
    role_name: DiskMonitoringAutomationRole
    regions:
      - eu-west-1
